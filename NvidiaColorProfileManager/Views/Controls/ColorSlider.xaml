<UserControl x:Class="NvidiaColorProfileManager.Views.Controls.ColorSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root">
    <Grid Margin="0,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="95"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" MinWidth="55"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{Binding Header, ElementName=Root}"
                   VerticalAlignment="Center"
                   FontSize="13"
                   Foreground="#BBBBBB" />

        <Slider Grid.Column="1"
                Margin="14,0"
                Minimum="{Binding Minimum, ElementName=Root}"
                Maximum="{Binding Maximum, ElementName=Root}"
                Value="{Binding Value, ElementName=Root}"
                TickFrequency="{Binding TickFrequency, ElementName=Root}"
                IsSnapToTickEnabled="True" />

        <TextBlock Grid.Column="2"
                   VerticalAlignment="Center"
                   FontSize="12"
                   Foreground="#999999">
            <Run Text="{Binding Value, ElementName=Root, StringFormat={}{0:F2}}" />
            <Run Text="{Binding Unit, ElementName=Root}" />
        </TextBlock>
    </Grid>
</UserControl>
